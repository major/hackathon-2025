# Radon configuration file
# This prevents radon from trying to parse pyproject.toml which has pytest config that breaks ConfigParser

[radon]
# Exclude patterns
exclude = tests/*,__pycache__/*,.venv/*

# Complexity thresholds
cc_min = C
mi_min = A
